# Claude Code開発フロー指示書

## 概要
この指示書は、Claude Codeに開発タスクを依頼する際の標準的なフローを定義しています。Clean Architectureの原則に従い、体系的で品質の高い開発を実現するためのガイドラインです。

## 🚨 重要な前提条件
Claude Codeは以下の手順を**必ず**実行する必要があります。手順の省略は開発品質の低下につながります：
- **🔍 実装前の詳細計画出力（必須）** - タスク着手前に必ず計画を出力・確認
- **✅ 全テストの完全成功確認（必須）** - `go test ./...`が100%成功するまで修正継続
- タスクチケットの作成（`tasks/`ディレクトリ）
- フィーチャーブランチの作成
- ドキュメント更新（README.md, CLAUDE.md, TEST_PLAN.md）
- コミット作成とマージ

## 基本指示フォーマット

```
[タスク名]をDevelopment Workflowに従ってください。
```

**例:**
- `Task9をDevelopment Workflowに従ってください。`
- `認証システムの実装をDevelopment Workflowに従ってください。`
- `APIレート制限機能の追加をDevelopment Workflowに従ってください。`

## Development Workflow詳細

### 1. 計画・設計フェーズ
Claude Codeは以下を**必ず**実行します：

#### ✅ 必須チェックリスト
- [ ] **既存コードベースの調査**
  - `tasks/`ディレクトリの実装計画書確認
  - 既存アーキテクチャパターンの分析
  - 依存関係の理解

- [ ] **🚨 実装計画の詳細出力（必須）**
  - 実装する機能の詳細設計を文章で出力
  - データベース設計、API設計、アーキテクチャ設計の明確化
  - 実装順序と各ステップの説明
  - 想定されるテストケースとテスト戦略
  - **この計画出力なしに実装開始は禁止**

- [ ] **タスクチケット作成**
  - `tasks/task[X].md`ファイルの作成
  - 実装仕様、API設計、テスト計画の記載
  - 既存パターンとの整合性確認

- [ ] **タスク管理開始**
  - TodoWriteツールで詳細なタスク分解
  - 優先度とステータスの設定
  - 進捗の可視化

### 2. ブランチ戦略
```bash
git checkout -b feature/[task-name]
```

### 3. 実装順序（Clean Architecture準拠）
**依存関係ルール**: 内側の層は外側の層に依存しない

#### 段階的実装:
1. **Domain Layer（最内側）**
   - `internal/domain/entity/` - エンティティ定義
   - `internal/domain/repository/` - リポジトリインターフェース

2. **Infrastructure Layer（外側）**
   - `internal/infrastructure/repository/` - リポジトリ実装
   - `internal/infrastructure/database/` - DB設定更新

3. **Application Layer（ユースケース）**
   - `internal/usecase/` - ビジネスロジック実装

4. **Presentation Layer（最外側）**
   - `internal/presentation/handler/` - HTTPハンドラー
   - `internal/presentation/middleware/` - ミドルウェア
   - `internal/presentation/router/` - ルーティング設定

5. **Dependency Injection**
   - `cmd/main.go` - 依存関係の配線

### 4. テスト実装・品質保証
#### ✅ 必須チェックリスト
- [ ] **カスタムモック実装** - 外部ライブラリに依存しない
- [ ] **各レイヤーでのテスト作成** - 単体テスト・統合テスト
- [ ] **🚨 テスト完全成功確認（必須）** - `go test ./...`で100%成功必須
- [ ] **既存テスト修正** - 変更による影響の修正
- [ ] **エラーハンドリングテスト** - 異常系テストの実装

#### 🚨 テスト失敗時の対応（必須継続）
テストが失敗した場合、Claude Codeは以下を**必ず継続実行**：
1. **エラー内容の詳細分析** - 失敗原因の完全な特定
2. **段階的な修正実装** - 一つずつ確実に問題解決
3. **修正後の再テスト実行** - `go test ./...`で再度確認
4. **🔄 全テスト成功まで繰り返し** - 1つでも失敗があれば継続修正
5. **テスト成功なしに次ステップ進行禁止** - 100%成功が次段階の必須条件

#### ⚠️ テスト成功の定義
- `go test ./...` の実行結果で **「PASS」のみ表示**
- **「FAIL」や「ERROR」が一切ない状態**
- **全パッケージ・全テストケースの成功**
- **ビルドエラーやコンパイルエラーも解決済み**

### 5. ドキュメント更新
- **API仕様** - `CLAUDE.md`のAPIエンドポイント追加
- **実装パターン** - アーキテクチャ説明の更新
- **機能説明** - READMEの機能リスト更新

### 6. マージ戦略
```bash
git checkout task-documentation
git merge feature/[task-name]
```

## Claude Codeの自動実行プロセス

### Phase 1: Analysis & Planning 📊
- [ ] 既存コードベース分析
- [ ] タスク計画書確認（`tasks/`ディレクトリ）
- [ ] **🚨 詳細実装計画の出力（必須）**
  - 機能仕様の詳細説明
  - データベース設計の詳細
  - API設計とエンドポイント仕様
  - 実装順序とステップ詳細
  - テスト戦略とテストケース計画
- [ ] **タスクチケット作成**（`tasks/task[X].md`）
- [ ] TodoWriteでタスク分解・管理開始
- [ ] フィーチャーブランチ作成

### Phase 2: Clean Architecture Implementation
- [ ] Domain Layer実装（エンティティ・インターフェース）
- [ ] Infrastructure Layer実装（リポジトリ・DB）
- [ ] Application Layer実装（ユースケース）
- [ ] Presentation Layer実装（ハンドラー・ルーター）
- [ ] Dependency Injection（main.go配線）

### Phase 3: Quality Assurance ✅
- [ ] 包括的テスト実装（カスタムモック使用）
- [ ] **🚨 全テスト完全成功確認（必須継続）**（`go test ./...`）
  - テスト実行結果の詳細確認
  - 失敗テストの完全な特定と修正
  - **100%成功まで修正作業継続**
  - **次段階進行の絶対条件**
- [ ] **エラー修正・再テスト（失敗時必須継続）**
  - 失敗原因の根本的解決
  - 段階的修正とテスト実行の繰り返し
  - 全テスト成功まで作業継続
- [ ] ビルド確認（`go build cmd/main.go`）
- [ ] 既存テスト修正（必要に応じて）

### Phase 4: Documentation & Integration
- [ ] CLAUDE.md更新（API・機能説明）
- [ ] コミット作成（詳細なコミットメッセージ）
- [ ] task-documentationブランチへのマージ

## 品質基準

### コーディング規約
- **Clean Architecture準拠** - 依存関係ルールの厳守
- **エラーハンドリング** - 適切なバリデーションとエラー処理
- **トランザクション管理** - データ整合性確保
- **命名規約** - 一貫したインターフェース・実装命名

### テスト要件
- **カバレッジ** - 各レイヤーでの包括的テスト
- **モック実装** - 外部依存なしのカスタムモック
- **成功基準** - `go test ./...`で全テスト通過

### ドキュメント要件
- **API仕様** - 新しいエンドポイントの完全な文書化
- **実装パターン** - アーキテクチャ説明の更新
- **使用例** - 実用的な使用方法の説明

## 使用例

### 基本的な機能追加
```
認証機能をDevelopment Workflowに従ってください。
```

### 複雑なシステム実装
```
Task9: リアルタイム通知システムをDevelopment Workflowに従ってください。
```

### 既存機能の拡張
```
検索機能にフィルタリングオプションを追加する実装をDevelopment Workflowに従ってください。
```

## 期待される成果物

### 技術成果物
1. **完全に動作するフィーチャー** - テスト済み・本番準備完了
2. **Clean Architectureコンプライアンス** - 適切な層分離
3. **包括的テストスイート** - 高品質なテストカバレッジ
4. **完全なドキュメント** - API仕様・使用方法

### プロジェクト成果物
1. **詳細なコミット履歴** - 実装プロセスの透明性
2. **更新されたドキュメント** - 最新の機能・API仕様
3. **統合済みフィーチャー** - メインブランチへの安全なマージ

## トラブルシューティング

### よくある問題と具体的解決方法

#### テスト失敗（必須継続作業）
**症状:** `go test ./...`でテストが失敗する
**解決手順:**
1. **🚨 エラー詳細分析** - 失敗原因の完全な特定と理解
2. **🔧 段階的修正** - 一つずつ確実に問題解決
3. **🔄 再テスト実行** - 修正後の `go test ./...` 実行
4. **♻️ 全成功まで継続** - **1つでも失敗があれば1-3を繰り返し**
5. **🚫 成功なしに進行禁止** - 100%テスト成功が次ステップの絶対条件
6. **📝 修正内容記録** - 何を修正したかの詳細記録

#### テスト成功の確認方法
```bash
go test ./...
# 期待する結果例:
# ok      github.com/example/project/internal/domain    0.123s
# ok      github.com/example/project/internal/usecase   0.456s
# ok      github.com/example/project/internal/presentation  0.789s
# PASS (全てのテストが成功)
```

#### ビルドエラー
**症状:** `go build`が失敗する
**解決手順:**
1. `go mod tidy`で依存関係整理
2. 型エラーの特定・修正
3. import文の整理
4. パッケージ構造の確認

#### マージコンフリクト
**症状:** ブランチマージ時のコンフリクト
**予防策:**
- 定期的なdevブランチとの同期
- 小さな単位での実装・マージ

## 🎯 実行成功の判定基準

### ✅ 成功判定チェックリスト
実装が完了したと判断できる条件：

#### 必須条件（すべて満たす必要がある）
- [ ] **🚨 実装前計画が出力されている** - 詳細な実装計画の文章出力
- [ ] タスクチケット（`tasks/task[X].md`）が作成されている
- [ ] フィーチャーブランチが作成されている  
- [ ] **🚨 全テストが100%成功している** - `go test ./...`で失敗ゼロ
- [ ] **🚨 テスト失敗時の修正が完了している** - 全エラー解決済み
- [ ] アプリケーションがビルドできる（`go build cmd/main.go`）
- [ ] CLAUDE.mdにAPI仕様が追加されている
- [ ] TEST_PLAN.mdにテスト計画が追加されている
- [ ] README.mdに機能説明が追加されている
- [ ] コミットが作成されている
- [ ] devブランチにマージされている

#### 品質条件
- [ ] Clean Architectureの依存関係ルールに従っている
- [ ] 各レイヤーでテストが実装されている
- [ ] エラーハンドリングが適切に実装されている
- [ ] APIレスポンスが設計通りである

## まとめ

この開発フローにより以下が保証されます：

### ✅ 品質保証
- **Clean Architecture準拠** - 適切な層分離と依存関係
- **包括的テスト** - 各レイヤーでの十分なテストカバレッジ
- **エラーハンドリング** - 堅牢なエラー処理実装

### ✅ 開発効率
- **体系的プロセス** - 明確な手順とチェックリスト
- **自動化** - テスト実行・ビルド確認の自動化
- **並行作業** - 複数タスクの効率的な管理

### ✅ 保守性
- **完全なドキュメント** - API仕様・テスト計画・使用方法
- **一貫した構造** - 統一されたアーキテクチャパターン
- **明確な履歴** - 詳細なコミットメッセージと変更記録

### ✅ 拡張性
- **モジュラー設計** - 独立性の高いコンポーネント
- **インターフェース指向** - 変更に強い設計
- **将来対応** - 新機能追加への容易な対応

**このフローを使用することで、Claude Codeは常に高品質で保守性の高いコードを提供し、プロジェクトの長期的な成功を支援します。**